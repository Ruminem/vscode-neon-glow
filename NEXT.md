# NEXT

**여기까지 됨** — 빛나는 표면을 셋 더 붙였다. 커서가 놓인 심볼(`occurrenceGlow`),
거터의 변경 막대(`gutterGlow`), 괄호 짝 상자(`bracketMatchGlow`). 셋 다 지난 세션에
세운 "이 색이 어디서 오는가"를 통과한다 — 전부 테마가 이미 그 자리에 주는 색이다.

**그리고 기본값에 규칙이 생겼다. 이번 세션의 제일 큰 결정이다.** 색만 바꾸는 설정은
켜져 나가고, 일하는 동안 에디터가 하는 일을 바꾸는 설정은 꺼져 나간다. 그 선으로 가르면
정적 글로우 다섯(`findGlow` `selectionGlow` `occurrenceGlow` `gutterGlow`
`bracketMatchGlow`)이 켜지고 움직이는 셋(`cursorTrail` `saveShake` `caretArc`)만
남는다. **받는 사람 모두의 화면이 바뀌는 두 번째 변경이다** — 첫 번째가 글로우 공식을
다시 잡은 v0.9.9였다.

셋만 켜고 `findGlow`/`selectionGlow`를 놔두려다 그만뒀다. 그러면 "왜 occurrence는
켜져 있고 find는 꺼져 있냐"에 답할 문장이 없다. **기본값을 정할 때는 개별 항목이 아니라
경계를 정할 것.** 한 문장으로 말할 수 없는 경계는 다음 사람이 반드시 다시 물어본다.

릴리스는 v0.9.7부터 나갔고 이번 세션에 **v0.10.0부터 v0.11.0까지** 다섯이 나갔다.
v0.10.0과 v0.10.1은 마켓 CI가 막혀 웹으로 올렸고, v0.10.2와 v0.10.3은 CI가 직접
게시했고, v0.11.0에서 또 막혀 웹으로 올렸다 — 아래 마켓 이야기 참고. 페이로드는 v0.10.0에서 v0.10.3까지 그대로였고, **v0.11.0에서
물결선이 들어가며 바뀌었다** — 받는 사람은 재패치와 완전 재시작이 필요하다.

**VS Code가 거터 막대를 그리는 방식이 규칙 모양을 정했다.** `border-left-style: solid`만
주고 **폭을 안 준다.** 그래서 CSS 초기값 `medium`, 곧 3px이 그대로 폭이 된다. 그리고
글로우를 걸 `:before`는 `width: 0`이다. **넓이가 없는 상자의 그림자는 블러를 아무리
키워도 아무것도 안 그린다.** 여기서는 `spread`가 빛에 몸을 주는 일 자체를 맡는다 —
찾기는 색이 약해서 spread가 필요했고 이쪽은 상자가 비어서 필요하다. **`spread`가 붙는
이유가 둘이 됐고, 그건 표 한 줄에 안 들어가서 README 산문에 남긴 유일한 항목이다.**

**diff 에디터에서는 거터 막대가 아예 없다.** `.modified-in-monaco-diff-editor`가 걸리면
VS Code가 `display: none`으로 숨긴다. 소스 관리 뷰에서 파일을 클릭하면 diff로 열리므로,
**"켰는데 안 보인다"의 첫 번째 원인이 여기다.** 콘솔에서 확인할 때 한 번 밟았다. 일반
탭으로 열어야 보인다.

**괄호 짝 상자는 `minChroma`가 버렸을 색을 밝히는 첫 규칙이다.** 기본 테두리가 `#888`이라
문턱을 절대 못 넘는다. 그래도 밝히는 이유를 코드에 적어뒀다 — `minChroma`는 본문
텍스트가 빛나지 않게 하려고 있는 것이고, 화면에 많아야 둘이며 커서가 괄호에 놓인
동안에만 있는 상자에는 그 논리가 이어지지 않는다. 나중에 "왜 여기만 회색을 통과시켰지"
하고 헷갈릴 자리다.

**기본값을 켜자 검사 절반이 무의미해졌다.** "켜면 나온다"를 확인하던 것들인데, 기본값이
이미 그 값이라 노브가 아예 안 도착해도 통과한다. **노브 검사에 보내는 숫자를 전부
기본값과 다르게 바꿨다.** 그리고 `0`으로 끄는 길이 아무 검사에도 안 걸리게 됐는데, 이제는
그게 사람이 실제로 쓰는 설정이라 섹션을 하나 더 붙였다. **기본값을 바꿀 때는 그 값을
전제하던 검사를 같이 볼 것.**

**`tools/smoke.js`에 새 검사는 맨 뒤에 붙일 것.** `run()`이 페이로드가 붙잡고 있는 전역
스텁을 갈아치우므로, 중간에 스텁을 새로 만들면 앞서 만든 것들이 죽는다. 증상이 원인과 두
섹션 떨어진 자리에 나타난다 — save pulse와 toggle이 빨개져서 한참 엉뚱한 데를 봤다.
코드에 주석으로 남겨뒀다.

**두 README를 절반으로 잘랐다.** 591 → 286줄, 516 → 264줄. 지운 것은 "What it costs"
벤치 표 두 개, 토글 브리지 내부 구조, 컨텍스트 키 설계, 기능별 산문이다. **전부
`neon-glow.js` 주석에 이미 같은 내용이 있던 것들이다.** 같은 것을 두 곳에 적으면 한쪽이
반드시 낡는다 — 실제로 두 README 모두 **"마켓플레이스에 없고 앞으로도 올리지 않음"**을
그대로 싣고 있었다. v0.9.11이 올라간 지 한참인데.

**`CLAUDE.md`의 문서 규칙도 같이 고쳤다.** 안 고치면 다음 세션에 도로 불어난다. 기능
하나에 딸린 네 자리 중 "두 README의 산문"이 "`neon-glow.js`의 그 규칙 바로 위 주석"으로
바뀌었다. **README는 쓰는 사람 것, 소스 주석은 고치는 사람 것.**

**그다음 두 파일을 하나로 합쳤다.** `README.md` 한 파일에 영어가 먼저, 한국어판이
`## 한국어` 아래 통째로 이어진다(한국어 제목은 `###`부터). 548줄. 한 파일이면 언어 전환이
같은 페이지 안 점프라 링크가 깨질 일이 없고, 두 판본의 어긋남이 바로 아래 보인다.
배지와 스크린샷은 영어 절에만 둔다.

**앵커는 GitHub가 실제로 만든 것과 대조할 것.** 슬러그를 흉내 내 계산한 것은 확인이
아니다. `gh api repos/Ruminem/vscode-neon-glow/readme -H 'Accept: application/vnd.github.html'`로
렌더링된 HTML을 받아 `user-content-` id를 뽑으면 된다. 링크 14개가 한글 제목까지 전부
맞았다.

**파일을 지우면 그걸 가리키던 링크가 게시된 쪽에 남는다.** `README.ko.md`를 지운 순간부터
마켓 페이지의 `한국어` 링크가 404였다 — 마켓은 게시된 VSIX 안의 README를 쓰므로 릴리스
전까지 옛 링크를 그대로 보여준다. 그걸 치우려고 v0.10.2를 냈다.

**그런데 마켓은 한글 제목에 한글 id를 안 만든다.** 비ASCII를 다 지우고 남는 게 없으면
`section`, `section-1`, `section-2`…로 순서대로 번호를 붙인다. `## 한국어`는 `id=section`,
`### 설치`는 `id=section-4`가 됐다. 그래서 `[한국어](#한국어)`는 **GitHub에서는 점프하고
마켓에서는 안 한다.** 한국어 절 안의 목차 링크 여섯 개도 마켓에서는 죽어 있다. 영어
제목은 두 곳 id가 같아서 문제없다. **한 링크가 두 곳에서 다 통하려면 id가 ASCII여야
한다.**

확인법도 적어둔다. 마켓 페이지 HTML은 서버에서 그려 오고, 속성에 **따옴표가 없다** —
`<h2 id=which-tokens-glow>`. `id="`로 찾으면 못 찾고 "마켓은 제목에 id를 안 붙인다"는 틀린
결론을 낸다. 이번에 한 번 그랬다.

**그래서 `## 한국어`를 `## Korean`으로 바꿨다(v0.10.3).** 두 곳 다 `id=korean`이 된다.
`<a id="korean"></a>`을 제목 위에 두는 길도 있었는데, 마켓이 README 속 HTML `id`를
남기는지는 올려봐야만 알 수 있어서 확실한 쪽을 골랐다(GitHub 코드 검색은 `<a name=` 같은
기호를 색인하지 않아서 남의 확장으로 미리 볼 수도 없었다). 한국어 절 안의 목차 링크
여섯 개는 여전히 GitHub에서만 점프한다 — 알고 둔 것이다.

**커서 아크는 이 페이로드의 성격을 바꾼 첫 기능이다.** 지금까지는 전부 한 번 계산된
정적 CSS였는데, 이건 **작업하는 동안 에디터를 지켜본다** — 화면에 있는 모든 커서 레이어에
MutationObserver를 걸고, 커서의 인라인 `left`/`top`을 문자열로 읽어 비교한다(레이아웃
비용이 없다). 그리는 프레임에서만 사각형을 잰다. 만들면서 걸린 것 셋을 적어둔다.

- **`innerHTML`을 쓸 수 없다.** 워크벤치가 Trusted Types를 강제해서 문자열을 대입하면
  예외가 난다. 전부 `createElementNS`로 만들어야 한다. 지금까지 이걸 안 밟은 건
  `<style>`에 `textContent`만 넣고 있었기 때문이지 알고 피한 게 아니었다.
- **`.focused`는 붙잡을 수 있는 손잡이가 아니다.** 포커스가 패널이든 DevTools든 다른
  데로 가면 Monaco가 즉시 떼어간다. 게다가 `focusin`을 캡처 단계로 걸면 Monaco가 그
  클래스를 붙이기 **전에** 돌아서, 하필 절대 못 찾는 순간에 찾게 된다. 포커스로 대상을
  고르지 말 것.
- **dash가 경로보다 길면 애니메이션이 거꾸로 간다.** 불꽃을 나르는 `stroke-dashoffset`
  이야기다. 화살표 한 번은 경로가 7px인데 머리 길이 하한이 14px이라, 문턱값을 5로 내린
  순간 짧은 아크가 전부 역방향이 됐다. 머리를 경로 길이로 제한해야 한다.

**두 갈래를 구분하게 된 것이 이번 세션의 제일 큰 소득이다.** 효과를 늘리는 것(스캔라인,
부팅 연출)과 빛나는 표면을 늘리는 것(찾기 결과, 선택 영역)은 같은 "기능 추가"가 아니다.
앞쪽은 얹을 때마다 "테마에서 유도한다"는 한 문장에서 멀어지고, 뒤쪽은 그 문장을 그대로
연장한다. **다음에 뭘 넣을지 정할 때 이 질문부터 할 것 — 이 색이 어디서 오는가.**
커서 잔상이 통과한 것도 테마의 커서 색을 그대로 쓰고 움직임만 바꾸기 때문이었다.

**반투명한 테마 색은 스프레드가 필요하다.** 찾기와 선택 색은 글자 뒤에 깔리는 색이라
테마가 반투명으로 잡아둔다. 그 색을 `box-shadow`로 블러만 시키면 거의 보이지 않는다.
`spread`로 원래 폭만큼 먼저 밀어낸 다음 번지게 해야 읽힌다. 커서 색은 불투명이라
스프레드가 없다 — 나중에 왜 여기만 다른지 헷갈릴 자리라 코드에 주석으로 남겼다.

**값은 콘솔에서 골랐다.** DevTools 콘솔에 스타일을 주입하면 재패치도 재시작도 없이
진짜 에디터·진짜 테마 위에서 즉시 보인다. 커서 잔상을 130ms로 잡았다가 답답해서
80 → 60 → 45로 내려간 것이 그 자리에서 끝났다. **페이로드를 고치기 전에 콘솔에서 먼저
볼 것.** 말로 설명하면 감이 안 오고, 재패치를 거치면 한 번 볼 때마다 재시작이다.
찾기 결과를 처음 볼 때 `.cmd`의 `echo`로 시험한 것이 실패 사례다 — 이미 빛나는 키워드
위에 빛을 얹은 것이라 차이가 안 보였다. **안 빛나는 자리에서 시험할 것.**

**흔들림의 비용은 효과가 아니라 구현이 정한다.** 처음에는 "저장할 때마다 87ms 블러를
몇 프레임씩 되풀이한다"며 이 항목을 자르라고 했는데, 그건 `setInterval`로 인라인
transform을 쓰는 데모 기준이었다. `transform`만 건드리는 CSS 키프레임에 `will-change`를
붙이면 워크벤치가 잠깐 컴포지터 레이어로 올라가고, 150ms 동안 이미 가진 텍스처를
옮기기만 한다. 글로우는 한 번도 다시 그려지지 않는다. 눈에 보이는 효과는 같은데 비싼
구현과 공짜 구현이 갈린다.

**렌더러로 이벤트를 즉시 보낼 방법은 상태바 라벨 하나뿐이다.** 페이로드는 워크벤치 안에
살아서 `onDidSaveTextDocument`를 못 듣고, `state.json`은 800~1500ms 폴링이라 Ctrl+S에
대한 반응으로 쓸 수 없다. 그래서 라벨 뒤에 U+200B를 0~3개 붙여 보낸다. 폭이 0이고,
`trim()`에 지워지지 않고, 스크린 리더도 읽지 않는다. **VS Code가 이 문자를 DOM까지
살려 보내는지가 이번 작업의 유일한 미검증 위험이었고, 실기에서 통과했다.**

**스캔라인은 넣지 않았다.** 기술적으로는 제일 싸다 — 정적 오버레이 하나라 스크롤당
비용이 0이다. 넣지 않은 이유는 성능이 아니라 위의 두 갈래 구분이다. 부팅 연출도 같은
이유로 보류했고, 더해서 창을 열 때마다 값을 치른다 — 신기함은 한 번이고 지연은 매번이다.
**터미널 글로우**도 후보였는데, xterm이 캔버스에 그려서 CSS가 안 닿는다.
`terminal.integrated.gpuAcceleration: "off"`를 켜야 DOM 렌더러로 떨어지는데, 남의 설정을
요구하면서 터미널 성능까지 내리는 교환이라 접었다.

**마켓플레이스 게시는 자기 워크플로로 나간다** — `.github/workflows/marketplace.yml`.
릴리스 워크플로에 job을 더하지 않고 파일을 나눈 이유는 실패의 성격이 다르기 때문이다.
릴리스 에셋은 다시 빌드해 올리면 되지만, 마켓에 도달한 버전은 그 번호로 다시 못 올린다.

**그 판단이 첫 게시에서 바로 값을 했다.** 태그를 밀었을 때 아직 `VSCE_PAT` 시크릿이
없어서 마켓 워크플로가 10초 만에 실패했는데, 릴리스 워크플로는 그 옆에서 정상으로 돌아
에셋 셋을 다 붙였다. 시크릿을 넣은 뒤 **태그를 옮기거나 버전을 올리지 않고 같은
`v0.9.8` 태그에 `workflow_dispatch`로 다시 돌려서** 게시했다. 그 기능은 "토큰이
만료됐을 때 태그는 이미 밀린 지 오래"를 위해 넣었는데, 만료가 아니라 첫 설정에서 먼저
쓰였다.

앞으로는 **버전 올려 커밋하고 `node tools/release.js --push` 한 번이면 GitHub 릴리스와
마켓 게시가 같이 나간다.** 퍼블리셔는 `Ruminem`, 확장 ID는 `Ruminem.vscode-neon-glow`.
**PAT은 발급 시점부터 최대 1년이라 2027년 안에 만료된다** — 그때 게시만 빨갛게 실패하면
원인이 그거다. 새 토큰은 반드시 **All accessible organizations**로 발급할 것. 특정 조직으로
잡으면 401이 떨어지는데 메시지가 "토큰이 틀렸다"처럼 나와서 원인을 찾기 어렵다.

**`vsce publish`가 타임아웃 나면 웹으로 올릴 것.** v0.9.11에서 이걸로 하루를 썼다.

`vsce publish`가 `Request timeout: /_apis/gallery`로 죽었다. CI에서 다섯 번, 로컬에서
한 번, 14시간에 걸쳐서. **마켓 관리 화면에서 VSIX를 직접 올리니 한 번에 됐다.**

게시자 페이지(marketplace.visualstudio.com/manage/publishers/Ruminem)에서 확장 **이름
오른쪽의 `⋮`** → Update. 확장 하나를 눌러 들어간 통계 화면의 `...`에는 없다. 올릴
파일은 **릴리스에 붙은 vsix를 받을 것** — 다시 빌드하면 마켓과 릴리스가 다른 바이트가
된다.

**어제 세운 가설 두 개가 둘 다 틀렸다. 다음에 같은 데서 헤매지 말 것.**

- "인증 실패면 401이 즉시 온다" — **아니다.** 인증이 안 되면 갤러리는 302로 로그인
  페이지에 보내고, 클라이언트가 거기서 멎으면 **타임아웃처럼 보인다.** 토큰을 의심할
  거면 추측하지 말고 `vsce verify-pat Ruminem`을 쓸 것. 워크플로에 단계로 넣어뒀고,
  실제로 5초 만에 토큰 가설을 지웠다.
- "마켓 쓰기 경로가 죽었다" — **아니다.** 같은 시각 다른 게시자들은 3분·5분·7분 전에
  계속 올리고 있었다. 확인하는 법은 마켓 조회 API에 `filterType:8`(VS Code 대상),
  `sortBy:1`(최근 갱신순)로 물어보고 맨 위 항목들의 `lastUpdated`를 보는 것이다.
  **"나만 안 되는가"를 부작용 없이 가르는 유일한 방법이다** — 시험 삼아 다른 확장을
  진짜로 게시하는 것은 하지 말 것.

**왜 API만 막혔는지는 끝내 모른다.** 같은 계정, 같은 토큰(검증 통과), 같은 파일인데
경로만 다르면 갈렸다. 더 파려면 시험 게시를 해야 해서 거기서 멈췄다. 다음에 또 나면
**원인을 파지 말고 웹으로 올릴 것.**

**v0.10.0부터 `verify-pat` 자체가 죽었다 — 그리고 토큰 탓이 아니었다.** 3분 남짓 걸려
`Request timeout: /_apis/securityroles`. v0.10.0, v0.10.1 둘 다 같았다.

처음엔 토큰을 의심해서 **All accessible organizations로 새로 발급해 시크릿을 갈았는데
똑같이 죽었다.** 옛 토큰과 새 토큰이 같은 자리에서 같은 시간 걸려 죽으면 토큰 문제가
아니다. **재발급보다 "남도 이러는가"를 먼저 볼 것** — 재발급은 싸지만 결론을 주지 않는다.

**남도 같은 병을 앓고 있었다.** vscode-shellcheck의 semantic-release가 9/8 04:53부터 같은
에러로 계속 실패해서 자동 이슈(vscode-shellcheck#1926)가 열려 있다. 다른 계정, 다른
토큰, 같은 에러다. 9/7에는 vsce 리포에 같은 증상(microsoft/vscode-vsce#1328)이
올라왔다가 한 시간 만에 "되네"로 닫혔다. **Microsoft 쪽이 9/7부터 오락가락하는
것이다** — 우리 dry-run이 9/8 22:33에 12초 만에 통과한 것도 그 사이 멀쩡하던 틈이었다.

vsce 버전도 아니다. `@latest`는 `3.9.2`로 그대로였다(`3.9.3-N`은 `next` 태그라 CI가
안 쓴다). 인증 없이 그 엔드포인트를 찌르면 0.2초 만에 302가 온다 — 엔드포인트는 살아
있고, 토큰을 붙인 요청만 멎는다. 찾는 법도 적어둔다: `gh search issues`로 에러 문자열을
GitHub 전체에서 찾을 것. Git Bash에서는 `/_apis/...`가 Windows 경로로 바뀌어 쿼리가
깨지니 `MSYS_NO_PATHCONV=1`을 앞에 붙일 것.

**그리고 v0.11.0에서 또 막혔다.** 2026-09-11 15:52 UTC, `verify-pat`에서 3분 12초,
같은 `/_apis/securityroles`. 하루 사이에 됐다 안 됐다 한다는 뜻이니, **마켓 CI는 되면
좋고 안 되면 웹으로 올리는 것**으로 여길 것. 아래가 잘 됐던 날의 기록이다.

**v0.11.2에서는 그냥 통과했다.** 2026-09-12 05:28 UTC, verify-pat 몇 초, 게시까지 전체
23초. 하루 전 v0.11.0이 3분 12초에 죽은 그 자리다. 손댄 것은 아무것도 없다 — 오락가락한다는
말이 이런 뜻이다.

**v0.10.2에서 CI 게시가 다시 됐다.** 2026-09-10 14:01 UTC, verify-pat 12초, publish 2초 —
9/8의 멀쩡하던 틈과 같은 모양이다. 토큰은 그 사이 새로 발급한 것이지만 **토큰이 고친 게
아니다.** 같은 새 토큰으로 13:36에 돌린 dry-run은 똑같이 죽었다. Microsoft 쪽이 13:39와
14:01 사이 어딘가에서 돌아온 것이다. 오락가락한 이력이 있으니 **다음에도 마켓 CI가
빨개지면 먼저 dry-run으로 보고, 죽으면 웹으로 올릴 것.**

**나았는지 아는 법은 우리 dry-run 하나다.** `gh workflow run marketplace.yml --ref main` —
verify-pat이 몇 초에 통과하고 태그 검사에서 멈추면 된 것이다. 게시까지 안 가서 안전하다.
vscode-shellcheck#1926이 닫히는 것도 신호로 적어뒀었는데 **약한 신호였다** — 그쪽은
master에 푸시하고 테스트가 통과할 때만 게시를 다시 시도해서, 우리 게시가 통과한 뒤에도
9/9 이후 한 번도 다시 안 돌았다. 남의 CI를 신호로 쓸 때는 그게 언제 도는지부터 볼 것.

PAT을 우회하는 길도 적어둔다 — vsce 3.9.2의 `--azure-credential`(Microsoft Entra ID).
Azure에 서비스 주체와 GitHub OIDC 페더레이션을 만들고 게시자 멤버로 넣어야 해서 주말
하나로는 빠듯하다. 또 몇 주씩 막히면 그때 볼 것.

**v0.10.0의 셋은 실기에서 확인했다 — 창 다시 로드로는 안 됐다.** 재패치 뒤 창만 다시
로드했을 때는 새 스타일시트가 없었고, VS Code를 전부 끄고 켜니 괄호 짝 상자와 거터
막대가 빛났다. README '발목 잡는 것들' 첫 항목이 그대로 일어난 것이다. 그 전까지 돌던
페이로드는 도장이 `faadf5e7a61a`였는데 v0.9.11 이후 어느 커밋과도, LF로도 CRLF로도 안
맞았다 — 커밋된 적 없는 중간 상태에서 패치된 것으로 보인다. **도장이 이상하면 설치된
`workbench.js`에서 기능 이름(`occurrenceGlow` 같은)을 직접 찾아볼 것.** 도장을 캐는 것보다
빨리 답이 나온다.

**물결선(`squiggleGlow`)이 v0.11.0으로 나갔다. 위에 적었던 분석은 틀렸었다.** 평소 보이는
물결은 `border-bottom`이 아니라, VS Code가 테마의 `editorError/Warning/Info.foreground`로
만든 **SVG 배경**이다. `border-bottom: 4px double`은 고대비용 `-border` 색이라 대부분
테마가 비워둔다. 그래서 `filter: drop-shadow`가 물결 모양을 그대로 따라 빛나고, 이 요소는
글자 없이 물결만 담은 오버레이라 단어가 번지지 않는다. 오버레이는 보이는 줄에만 생기니
비용도 뷰포트에 묶인다 — "진단 개수에 비례해 안 묶인다"도 틀렸었다. 힌트(점 셋)는 VS
Code가 일부러 조용하게 두는 표시라 뺐다. **번들 CSS만 보고 판단하지 말 것** — 이 물결은
테마 참여자가 JS에서 규칙을 만들어 넣어서, CSS 파일에는 고대비 규칙만 있었다.

**콘솔로 넣은 스타일은 완전 재시작하면 사라진다.** 재시작 전에 넣은 스니펫을 재시작 뒤에
찾으면 없다 — 한 번 "안 빛난다"로 헷갈렸다. 콘솔 스니펫에는 `id`를 붙이고
`document.getElementById(...)?.remove()` 뒤에 다시 넣게 짤 것. 두 번 돌려도 겹치지 않는다.

**시험하려고 `neon-glow.js`를 고치지 말 것.** 거터 막대와 물결을 보려고 이 파일에 빈 줄과
`let x = ;`를 넣은 적이 있다. 빈 줄 하나만 커밋돼도 도장이 바뀌어 받는 사람 전원이 재패치를
해야 하고, 문법 오류가 섞이면 글로우 전체가 죽는다. **`Ctrl+N`으로 저장 안 한 새 파일**에서
볼 것 — 디스크에 안 남는다.

**도장 버그를 하나 고쳤다.** git이 Windows에서 `neon-glow.js`를 CRLF로 체크아웃하는데
도장은 바이트를 그대로 해시해서, 클론에서 `node install.js`로 패치하면 코드가 같아도
확장이 영원히 "옛 페이로드"라고 했다. `payloadStamp`가 해시 전에 `\r\n`→`\n`을 하게 했고,
LF로 찍힌 기존 설치는 도장이 그대로다(v0.10.3은 LF·CRLF 둘 다 `7a35ebcc5353`). smoke에
검사 둘이 들어갔다.

**설정 변경이 빠른 경로를 타게 했다.** 전에는 `state.json` 폴링만 탔고, 한가하면 1.5 →
3 → 6 → 12 → 15초로 늘어나서 슬라이더를 밀어도 최대 15초를 기다렸다. 이제 확장이
라벨에 표식(U+2060)을 하나 올리고 렌더러가 그걸 보면 `quiet = 0; pollState()`를 한다.
**표식은 저장 펄스(U+200B) 앞에 둬야 한다** — 렌더러가 펄스를 라벨 **끝에서부터** 세기
때문에, 뒤에 붙이면 그 개수가 0으로 읽혀서 설정을 바꿀 때마다 화면이 흔들린다. smoke에
검사 넷이 들어갔고, 펄스를 앞에서부터 세게 바꾸면 기존 저장 검사 둘까지 같이 빨개진다.
실기에서 즉시 반영되는 것을 확인했다.

**미리보기 명령을 만들었다가 뺐다. 이유를 남겨둔다.** 설정 옆에 샘플 파일을 열고 커서·
선택·가짜 진단까지 자리 잡아주는 명령(`neonGlow.preview`)을 다 만들어 돌려봤다. 그런데
**설정이 이미 열려 있는 파일에 즉시 반영되므로 자기 코드가 곧 미리보기다.** 화면 나누기·
커서 놓기·선택은 손으로 하면 되고, 미리보기만 할 수 있는 건 **물결선 스테이징 하나**였다
— 깨끗한 코드에는 에러가 없으니까. 그것도 `let x = ;` 넣고 `Ctrl+Z`로 대신된다. 값 하나에
`extension.js` 141줄과 문서 네 자리를 영구히 이고 갈 이유가 없었다. **밀기 전에 지웠으니
히스토리에도 없다** — 릴리스 노트에 "넣었다"와 "뺐다"가 나란히 박히지 않게.

교훈은 한 줄이다. **기능을 붙이기 전에 "손으로 하면 얼마나 번거로운가"를 먼저 물을 것.**
번거로움이 한 가지뿐이면 그 한 가지만 해결하는 편이 싸다.

**README 첫 화면이 그림 셋에서 하나가 됐다(v0.11.2 → v0.11.3).** `glow-off.png`,
`glow-on.png`, 그리고 움직이는 클립 하나였던 것이 **좌(글로우 없음)/우(글로우)로 합친 한
장**이 됐다. 900KB 세 파일이 770KB 한 파일이다. 지운 미리보기 명령이 남긴 그 샘플 파일이
화면에 그대로 쓰였다 — 첫 줄 주석이 "Leave this open beside Settings"라 README에서는 받는
사람이 없는 문장이다. **다시 찍을 일이 생기면 그 줄부터 지울 것.**

**캡처 도구가 기본으로 뽑는 GIF는 프레임 차분을 안 한다.** 7.41MB짜리에 `gifsicle -O3`을
무손실로 먹이니 1.22MB가 됐다. 84%다. `--lossy`나 `--colors`를 얹어도 0.79MB까지밖에 안
내려간다 — **덩치는 색이 아니라 중복 프레임이었다.** 앞 프레임과 똑같은 29장이 통째로 들어
있었고, 지워도 총 길이는 10.1초 그대로다. **GIF를 받으면 압축을 의심하기 전에 `-O3`부터
먹일 것.**

**그런데 같은 클립을 APNG로 뽑으니 0.64MB였다. GIF 최적화본의 절반인데 풀컬러다.** 어두운
배경에 변하는 영역이 좁아서 PNG 압축이 잘 먹는다. **README 애니메이션은 APNG를 먼저 볼
것** — 256색 밴딩 걱정이 같이 사라진다. ScreenToGif에서는 인코더 프리셋 목록이 아니라 **그
위의 파일 형식 드롭다운**에 있다. 파일 이름은 `.png`로 둘 것 — APNG는 정식 PNG라
GitHub·마켓·브라우저가 그냥 이미지로 받아 재생하고, `.apng`는 아무 데서도 안 먹는다.
**윈도우 사진 앱이 첫 프레임만 보여주는 것은 파일이 깨진 게 아니다** — 여기서 한 번
"재생이 안 된다"로 헤맸다. 확인은 Edge나 VS Code 미리보기로 할 것.

**녹화는 30fps다. `cursorTrail` 기본값 45ms는 한 프레임 반이라 잔상이 안 남는다.**
움직이는 셋을 찍을 거면 찍는 동안만 값을 올릴 것. 그러면 기본값이 아닌 화면이니 캡션에
적어야 한다. `caretArc`와 `saveShake`(150ms)는 기본값으로도 프레임이 충분하다.

**그림은 VSIX에 안 실린다.** `.vscodeignore`의 `images/**`에 걸리고, README는
`raw.githubusercontent.com`의 `main`을 가리킨다. **그래서 파일만 갈아끼우면 릴리스 없이
마켓 페이지의 그림이 바뀐다** — 단 README 마크업(=URL)이 바뀌면 그때는 릴리스가 필요하다.
이번엔 URL이 바뀌어서 v0.11.2를 냈다.

**뒤집으면 이렇다 — `main`에서 이미지를 지우면 게시된 마켓 README가 그 순간 깨진다.**
마켓은 게시된 VSIX 안의 README를 쓰는데 그림은 `main`에서 가져오므로, 파일을 지운 순간부터
다음 릴리스까지 옛 README가 없는 파일을 가리킨다. `README.ko.md`를 지웠을 때 한 번 밟았고
이번이 두 번째다. **이미지 삭제와 릴리스는 한 묶음으로 할 것.** 그래서 정적 둘을 지운
직후에 v0.11.3을 냈다.

**좌우 비교는 한 파일이어야 한다.** 애니메이션 이미지 둘을 나란히 놓으면 동기를 맞출 길이
없다 — 각 이미지가 디코드를 끝낸 시점에 자기 타이머로 재생을 시작하고, 둘을 묶어줄 공통
시계가 없다. 이번 두 클립은 길이마저 10.4초와 8.6초라 한 바퀴 안에 눈에 띄게 어긋난다.
그래서 `tools/hstack-apng.js`를 만들어 **한 파일에 좌우를 담았다.** 한 파일이면 시계가
하나라 어긋날 수가 없다. 짧은 쪽이 전체 길이를 정하게 했다 — 한쪽을 마지막 프레임에
세워두면 하필 볼 만한 절반이 멈추고, 안에서 되감으면 클립 중간에 되감기가 보인다.

**APNG가 커지는 원인은 압축이 아니라 무엇을 쓰고 있느냐다.** 처음 합성본이 2.44MB였다.
스캔라인 적응 필터를 붙여도 2.36MB로 거의 안 줄었다. 진짜 원인은 **바뀐 사각형 안의 안
바뀐 픽셀까지 통째로 다시 쓰고 있던 것**이었다. 블렌드를 `OVER`로 두면 투명 픽셀이 아래를
그대로 두므로 그 자리를 0으로 비웠더니 **0.77MB**가 됐다. 원본 둘의 합(0.12+0.64)과 같은
값이니 합성 자체는 비용을 안 더한다는 뜻이다. **PNG가 크면 deflate를 의심하기 전에 프레임
하나가 실제로 몇 픽셀을 바꾸는지 볼 것.**

**도구가 그 그림을 만드는지는 해시로 확인했다.** `tools/hstack-apng.js`를 원본 둘에 다시
돌려 나온 파일이 커밋된 `images/neon-glow.png`와 바이트까지 같다. **원본 두 클립은 커밋하지
않았다** — 다시 찍으면 어차피 새로 뽑는 것이고, 저장소에 0.76MB를 영구히 얹을 값이 없다.

**오후에 표면 넷이 더 붙고 버그 셋이 잡혔다. v0.11.2부터 v0.13.6까지 열여섯이 나갔고
마켓 CI는 열네 번 연속 통과했다** — "되면 좋고 안 되면 웹으로"는 그대로 두되, 9월 초의
그 며칠이 예외였던 쪽에 가깝다.

**제일 값있는 것부터. 패치를 찾는 창이 페이로드보다 작아졌다.** `patch.js`가 번들 끝에서
64KB만 읽어 표식과 도장을 찾는데, 페이로드가 70KB가 됐다. 증상이 고약하다 — 번들은
완벽하게 패치돼 있고, 확장은 "패치 안 됨"이라 하고, **다시 패치해도 재시작해도 안 고쳐진다.**
고칠 게 없기 때문이다. 창을 256KB로 넓혔다. **이 부류는 천천히 나빠지지 않고 어느 릴리스에서
통째로 온다** — 4.8KB 차이로 넘었다. smoke가 이제 필러 1MB 뒤에 진짜 페이로드를 붙인 가짜
번들을 만들어 `patch.js`에게 찾아보라고 시킨다. **페이로드에 뭔가를 더할 때 이 검사를 볼 것.**

**커서 아크가 한 칸 뒤에서 출발하고 있었다. `cursorTrail`을 켠 사람만 볼 수 있었다.**
관찰자는 인라인 스타일이 **바뀌는 순간**에 도는데, 그건 전이가 **시작하는** 순간이라 화면의
캐럿은 아직 떠나는 자리에 있다. 거기서 `getBoundingClientRect()`를 재면 옛 위치가 나온다.
`offsetParent`의 모서리 + 인라인 `left`/`top`으로 **목적지**를 계산하게 고쳤다 — 인라인 값은
무엇이 애니메이션하든 도착지를 들고 있다. **전이가 걸린 요소의 사각형은 지금 그려지는 자리지
가려는 자리가 아니다.**

**세로·대각 아크는 원래 안 그려지고 있었다.** `Math.abs(dy) >= 1`이면 문턱값을 보기도 전에
반환했다. 경로가 수평 폴리라인 하나뿐이라 못 그리는 걸 거절한 것인데, **설정 설명은 처음부터
"anything that moves it"을 약속하고 있었다.** 경로를 `+x`로 만들고 `rotate(atan2)`로 돌리는
것으로 바꿨다. 스크롤이 캐럿을 옮기는 경우는 컨테이너의 인라인 스타일을 같이 읽어 가른다.

**드래그는 포인터 이벤트로 읽어야 한다.** 클릭과 드래그는 버튼을 누른 채 캐럿을 옮기는 같은
동작이고, 드래그가 계속된다는 것만 다르다 — 그래서 누른 뒤 첫 이동만 그리고 나머지는
`caretArcOnDrag`가 열어준다. 처음엔 `mouseup`으로 읽었는데 **창 밖에서 버튼을 떼면 그게 안
온다.** 그러면 버튼이 영원히 눌린 상태가 되고 키보드 아크까지 죽는다. Monaco가 포인터
캡처를 잡으므로 `pointerup`은 도착하고, `blur`를 백스톱으로 뒀다.

**중단점 색은 테마 변수로 공개돼 있지 않다.** `debugIcon.breakpoint.foreground`를 설치본
전체에서 찾으면 0건이다. 그 이름으로 규칙을 썼으면 **소스에서는 멀쩡해 보이는데 아무것도 안
그리는 규칙**이 나갔다. `currentColor`로 가면 VS Code가 칠한 색을 그대로 쓰므로 이름을 알
필요가 없고 테마가 덮어쓴 색까지 따라간다. **규칙을 쓰기 전에 그 변수가 실제로 있는지 볼
것** — 물결선 때 배운 "번들 CSS만 보고 판단하지 말 것"의 다른 얼굴이다.

**줄 전체 틴트는 diff에서 거절하고 디버그에서 허락했다. 기준은 한 화면에 몇 개냐다.** diff는
바뀐 줄이 연달아 붙어 블러가 겹쳐 안개가 되고, 멈춘 줄·`rangeHighlight`·`symbolHighlight`는
한 번에 하나다. 괄호 짝 상자를 `minChroma` 예외로 통과시킨 것과 같은 논리다.

**`occurrenceGlow`는 절반이 빠져 있었다.** 언어 서버가 답하는 셋(`wordHighlight` 계열)만
밝히고 VS Code가 스스로 칠하는 `.selectionHighlight`를 빼먹어서, **키보드로 단어에 커서를
올리면 빛나고 더블클릭으로 고르면 안 빛났다.** 설정을 늘리지 않고 그 규칙 안에 넣었다.

**글로우 자체를 숨쉬게 하는 건 두 벽에 막힌다.** 하나, `@keyframes` 안의 `!important`는
무시되므로 `box-shadow`를 직접 애니메이션하면 기존 규칙에 진다. 둘, `text-shadow`는 값이
바뀌면 그 프레임에 블러를 다시 래스터한다 — 3겹 87ms 대 예산 16ms. **그 요소에
`filter: brightness`를 거는 것**이 답이다. 블러는 한 번만 래스터되고 프레임마다 결과만
어두워진다. `saveShake`가 `transform`으로 맺은 거래와 같다.

**그리고 무엇을 숨쉬게 할지의 기준은 개수가 아니라 화면에 머무는 시간이다.** 찾기 결과·멈춘
줄·괄호 짝 상자 셋은 **네가 그것을 기다리는 동안만** 존재한다. 심볼 강조는 개수로는 통과하지만
커서가 단어에 놓일 때마다 생겨서 일하는 시간의 대부분을 차지한다 — **항상 맥동하는 표시는
신호가 아니라 배경이다.** 그래서 뺐고, 그 이유를 소스와 설정 설명 양쪽에 적어뒀다.

**아크 스타일이 넷에서 열둘이 됐다.** 다섯을 더할 때 모양만 다섯 개 만들면 서로 닮아버린다.
축이 셋이다 — 점들이 앉는 곡선(`wave` `bolt` `coil` `square`), 대시가 경로를 쓰는 방식
(`dots` `zip` `pulse`), 터짐의 형태(`flash` `ring`). **뒤의 둘은 새 기하가 전혀 필요 없었다.**

**설정 화면에서 새 옵션이 안 먹으면 재시작부터 의심할 것.** 확장을 갈아끼우면 드롭다운은 바로
새것이 되지만 페이로드는 창이 뜰 때 한 번 읽힌다. 그러면 **새 단어를 골라도 렌더러가 모르는
값이라 버리고 쓰던 값을 유지한다** — "셋 다 arc랑 똑같다"로 보인다. `state.json`에 그 노브가
있는지 보면 확장 쪽이 새것인지 알 수 있고, VS Code 프로세스 시작 시각과 `workbench.js`
수정 시각을 비교하면 렌더러 쪽이 새것인지 알 수 있다.

**다음 할 것.** **`neonGlow.preset` 퀵픽** — 설정이 스물셋이 돼서 새로
깐 사람이 뭘 만져야 할지 모른다. 은은/기본/다 켜기 셋이면 되고, 값이 `state.json`을 타고
가므로 **페이로드를 안 건드려 재패치가 없다.** 제일 싸다. **설정 설명 번역** —
`package.nls.json`/`package.nls.ko.json`이면 한국어 VS Code에서 설정 설명과 명령 이름이
한국어로 뜬다. README보다 자주 보이는 자리다. README 언어를 늘리는 건 하지 않기로 했다 —
한 파일 방식은 둘까지가 맞다.

**그때 쓸 용어의 출처는 언어팩이다.** `~/.vscode/extensions/ms-ceintl.vscode-language-pack-ko-*/translations`를
grep하면 VS Code 자신이 그 UI 요소를 뭐라고 부르는지 나온다. 설정 설명은 VS Code의 한국어
문구 바로 옆에 놓이므로, README와 달리 어긋나면 한 화면에서 보인다. 한국어 절을 이걸로
한 번 훑어서 `괄호 짝 상자`를 `일치하는 괄호 상자`로 고쳤다 — `editorBracketMatchBackground`가
"일치하는 괄호 뒤의 배경색"이다.

**그 김에 배운 것이 본론이다. 비슷해 보이는 두 단어가 실은 두 기능이었다.** 같은 README에
`괄호 쌍 색칠`과 `괄호 짝 상자`가 같이 있어서 처음엔 일관성 문제로 보고 한쪽으로 묶으려
했는데, `괄호 쌍`은 bracket pair colorization이고 `괄호 짝`은 `bracketMatchGlow`다. 묶었으면
**서로 다른 두 기능이 같은 이름을 갖게 될 뻔했다.** 용어를 통일하기 전에 그것들이 정말 같은
것을 가리키는지부터 볼 것. `심볼`(팩은 "기호")과 `찾기 결과`(팩은 "검색 일치 항목")는 알고
그대로 뒀다 — 팩 쪽이 한국어로 더 헷갈리거나, 사용자가 보는 위젯 이름과 다르다.

**여전히 맥북에서 macOS 경로를 돌려보는 것도 남아 있다.** 다섯 세션째 바뀐 게 없다.
서명된 `.app` 내부를 고친 뒤 Gatekeeper와 hardened runtime이 어떻게 반응하는지가 유일한
미지수고, "손상되어 열 수 없음"이 뜨면 macOS 지원은 접어야 한다.

**`tools/smoke.js`도 들어왔다** — 네 세션 동안 만들었다 버리기를 반복하던 그것이다.
스텁 DOM에 페이로드를 올려 22가지를 검사하고, 실패하면 종료 코드가 1이다. 넣자마자
값을 했다: 폴링 백오프를 붙이면서 `document.addEventListener`를 페이로드 한가운데
무방비로 놓았는데, 거기서 던지면 **그 뒤 첫 페인트까지 통째로 죽는다**는 것을 커밋 전에
잡아냈다. 그 상황이 검사 항목으로 박혀 있다(`crippled` 스텁).

**통과만 하는 검사는 검사가 아니므로 확인했다.** 가드를 일부러 떼고 돌려서 그 항목이
실제로 빨개지는 것을 보고 되돌렸다. 새 검사를 넣을 때도 같은 절차를 밟을 것.

**워크플로 CI는 태그 없이도 시험할 수 있다.** 마켓 워크플로를 `main`에 대고
`workflow_dispatch`로 돌리면 체크아웃과 setup-node를 실제로 태운 뒤 태그 검사에서
멈춘다 — 게시까지 가지 않으므로 되돌릴 수 없는 일이 일어나지 않는다. 액션 버전을
v4에서 v7로 올릴 때 이 방법으로 확인했다. 일부러 빨간 실행이 하나 남지만, 릴리스를
내보내고 나서야 CI가 깨진 걸 아는 것보다 낫다.

**글로우에서 눈에 보이는 것은 총량이 아니라 비율이다.** 네 겹이 전부 글자 바로 옆에도
칠하는데(블러는 원본 자리에서 가장 진하다), 옛 알파는 거기서 2.75로 더해졌다. 1.0을
넘으면 다 같은 불투명이라 **가장 넓은 겹이 가장 좁은 겹만큼 진했고**, 그래서 눈이 경계를
찾을 기울기가 없었다. 밝기를 도로 올려도 선명함이 유지되는 것을 보고 확인했다 — 어두워져서
좋아진 게 아니었다. 다음에 글로우 모양을 손볼 일이 있으면 **총량 말고 안팎 비율을 볼 것.**

**`tools/bench.js`를 만들었다.** CDP로 스크롤을 구동하면서 두 공식을 번갈아 적용하고
이웃한 쌍끼리 비교한다. 의존성 없이 노드 내장 `WebSocket`과 `fetch`만 쓴다. 이번 측정은
네 쌍 전부 −56~−57%로 흔들림이 없었다.

두 함정이 시간을 먹었으니 적어둔다. 하나, **VS Code 통합 터미널에는
`ELECTRON_RUN_AS_NODE=1`이 걸려 있다.** 그 상태로 `Code.exe`를 띄우면 에디터가 아니라
Node로 뜨고, 모든 VS Code 플래그에 "bad option"을 뱉으면서 즉시 죽는다 — 플래그가 틀린
것처럼 읽혀서 한참 헤맸다. 둘, **`/json/list`는 `id`, CDP 명령은 `targetId`다.** 같은
대상을 다르게 부르고, 틀리면 필드 이름이 없는 역직렬화 오류만 나온다.

**막힌 것** — macOS/Linux 실기가 없다. **Windows 11에서만** 검증됐고, 나머지 두
플랫폼은 여전히 "동작하도록 작성했을 뿐"이다. 물결선은 v0.11.0 이후 실기에서 확인했다.
아직 못 본 규칙은 **디버그 중 멈춘 줄과 중단점 글로우** 하나뿐이다 — 실제로 디버깅을
돌려야 뜨는 것들이라 그 자리만 비어 있다.

**그리고 DOM으로 닿는 표면은 사실상 끝났다.** 남은 것은 캔버스라 CSS가 못 간다 —
터미널(xterm), 미니맵, 오버뷰 룰러. 그 밖은 워크벤치 크롬(탭의 수정 점, 활동 표시줄 배지,
상태바 오류 개수)인데, 그건 "구문 강조에 글로우를 입힌다"에서 한 걸음 벗어나는 쪽이라
**할지 말지가 취향 문제**가 된다. 표면을 더 찾는 대신 `tools/live.js` 쪽으로 가는 게 맞아
보인다.

**보류** — 로더 방식. 번들에는 로더만 심고 실제 페이로드는 확장 폴더에서 `vscode-file`로
불러오면 재패치가 영구히 사라진다. 대신 시작할 때 페이로드를 가져와야 해서 글로우가 늦게
뜨는 구간이 생긴다. 자주 겪는 불편을 줄이려다 항상 겪는 불편을 만드는 교환이라 그대로
보류다.

**그리고 재보려다 더 앞에 있는 미지수를 찾았다. 렌더러가 그 파일을 읽을 수 있는가.**
`tools/loader-cost.js`를 만들어 확장 폴더의 페이로드를 `vscode-file`로 페치해봤는데,
스크래치 프로필 창에서는 **확장 폴더도 globalStorage도 `Failed to fetch`**였다. 스크래치
프로필이라 그런 것인지(그 창에서는 진짜 프로필 아래가 통째로 안 읽힌다 — `bridgeOk`가
false인 이유가 이것이다) 확장 폴더라는 뿌리 자체가 막힌 것인지는 **정상 프로필 창에서
다시 돌려봐야 갈린다.** 도구가 그 둘을 구분해서 말하도록 적어뒀다.

**확장 폴더가 막힌 것이면 로더는 죽은 게 아니라 자리를 옮기면 된다** — `state.json`이
매 세션 매초 읽히는 globalStorage는 읽을 수 있는 뿌리임이 증명돼 있으니, 설치할 때
페이로드를 거기에 복사해두고 로더가 그쪽을 읽으면 된다. **깜빡임을 재기 전에 이것부터
확인할 것.** 순서가 뒤집혀 있었다.

**재는 법도 적어둔다.** 로더가 더하는 것은 페이로드 페치 한 번뿐이고, 그 뒤의 기다림
(`.vscode-tokens-styles`가 채워지기를 기다리는 것)은 지금과 똑같다. 그래서 볼 것은
**페치 시간 대 첫 페인트 시간**이다. 페치가 그 대기 안에 묻히면 깜빡임은 0이다.

**그런데 그 교환의 한쪽을 아무도 안 쟀다.** 깜빡임이 몇 ms인지 모른 채 접혀 있다 —
"성능을 단일 측정으로 못 믿는다"를 규칙으로 들고 있는 저장소에서 **측정이 아예 없이**
내려진 판단이다. 게다가 페이로드는 이미 `.vscode-tokens-styles`에 내용이 차기를 기다렸다가
그리므로, 페치가 그 대기 안에 통째로 묻힐 수도 있다. 그러면 깜빡임이 0이다. **재보고
결론을 내면 보류 항목 하나가 사라진다.**

**그리고 그걸 재려면 먼저 `tools/live.js`가 필요하다.** `tools/bench.js`가 이미 의존성 없는
CDP 클라이언트, 타깃 붙기, `Runtime.evaluate`, `Input.dispatchMouseEvent`를 갖고 있고
(`ELECTRON_RUN_AS_NODE` 함정과 `id` vs `targetId` 함정도 주석에 박혀 있다), 빠진 것은 CDP
호출 세 개 수준이다 — 키 입력, 마우스 이동, 스크린샷.

**핵심은 패치하지 말고 `Runtime.evaluate`로 페이로드를 부어 넣는 것이다.** 페이로드는
`window.__NEON_INSTALLED`로 가드된 IIFE라 이미 떠 있는 워크벤치에 그냥 실행하면 된다.
그러면 **재패치와 완전 재시작이 루프에서 사라진다** — 스크래치 인스턴스를 하나 띄워두고
고칠 때마다 다시 부으면 되고, 일하는 창은 건드리지 않는다. `NEXT.md`가 이미 "콘솔에서 먼저
볼 것"이라고 적어둔 그 동작의 자동화다.

**그림보다 단언이 먼저다.** 구동한 뒤 DOM을 읽어 확인하면 검사가 되고, 커밋을 막을 수 있다.
오늘 실기 확인 없이 나간 것들이 정확히 여기서 잡혔을 것이다 — Down에 `rotate(1.57rad)`짜리
요소가 생기는가, 드래그 중에 하나만 생기는가, 휠을 굴리면 아무것도 안 생기는가. **그리고
오늘 끝내 답을 못 얻어 방어적으로 막아둔 질문 하나가 여기서 풀린다** — 휠을 굴리고 `.cursor`의
`style.top`을 읽어보면, Monaco가 스크롤 때 그 값을 다시 쓰는지 한 번에 알 수 있다. 지금은
어느 쪽인지 모르는 채 가드를 달고 있다.

**못 하는 것도 적어둔다.** 주입 방식이라 **패치 경로·도장 검사·부팅 순서는 못 본다.** 그건
여전히 실기 재시작으로만 확인된다. `smoke.js`가 스텁 DOM을, `live.js`가 진짜 Monaco를,
패치 자체는 손으로 — 셋으로 나뉘는 게 맞다.
