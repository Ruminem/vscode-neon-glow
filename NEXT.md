# NEXT

**여기까지 됨** — 빛나는 표면을 셋 더 붙였다. 커서가 놓인 심볼(`occurrenceGlow`),
거터의 변경 막대(`gutterGlow`), 괄호 짝 상자(`bracketMatchGlow`). 셋 다 지난 세션에
세운 "이 색이 어디서 오는가"를 통과한다 — 전부 테마가 이미 그 자리에 주는 색이다.

**그리고 기본값에 규칙이 생겼다. 이번 세션의 제일 큰 결정이다.** 색만 바꾸는 설정은
켜져 나가고, 일하는 동안 에디터가 하는 일을 바꾸는 설정은 꺼져 나간다. 그 선으로 가르면
정적 글로우 다섯(`findGlow` `selectionGlow` `occurrenceGlow` `gutterGlow`
`bracketMatchGlow`)이 켜지고 움직이는 셋(`cursorTrail` `saveShake` `caretArc`)만
남는다. **받는 사람 모두의 화면이 바뀌는 두 번째 변경이다** — 첫 번째가 글로우 공식을
다시 잡은 v0.9.9였다.

셋만 켜고 `findGlow`/`selectionGlow`를 놔두려다 그만뒀다. 그러면 "왜 occurrence는
켜져 있고 find는 꺼져 있냐"에 답할 문장이 없다. **기본값을 정할 때는 개별 항목이 아니라
경계를 정할 것.** 한 문장으로 말할 수 없는 경계는 다음 사람이 반드시 다시 물어본다.

릴리스는 v0.9.7부터 나갔고 이번이 **v0.10.0**이다. 마켓플레이스에도 같이 올라간다
(첫 마켓 게시는 v0.9.8이었다).

**VS Code가 거터 막대를 그리는 방식이 규칙 모양을 정했다.** `border-left-style: solid`만
주고 **폭을 안 준다.** 그래서 CSS 초기값 `medium`, 곧 3px이 그대로 폭이 된다. 그리고
글로우를 걸 `:before`는 `width: 0`이다. **넓이가 없는 상자의 그림자는 블러를 아무리
키워도 아무것도 안 그린다.** 여기서는 `spread`가 빛에 몸을 주는 일 자체를 맡는다 —
찾기는 색이 약해서 spread가 필요했고 이쪽은 상자가 비어서 필요하다. **`spread`가 붙는
이유가 둘이 됐고, 그건 표 한 줄에 안 들어가서 README 산문에 남긴 유일한 항목이다.**

**diff 에디터에서는 거터 막대가 아예 없다.** `.modified-in-monaco-diff-editor`가 걸리면
VS Code가 `display: none`으로 숨긴다. 소스 관리 뷰에서 파일을 클릭하면 diff로 열리므로,
**"켰는데 안 보인다"의 첫 번째 원인이 여기다.** 콘솔에서 확인할 때 한 번 밟았다. 일반
탭으로 열어야 보인다.

**괄호 짝 상자는 `minChroma`가 버렸을 색을 밝히는 첫 규칙이다.** 기본 테두리가 `#888`이라
문턱을 절대 못 넘는다. 그래도 밝히는 이유를 코드에 적어뒀다 — `minChroma`는 본문
텍스트가 빛나지 않게 하려고 있는 것이고, 화면에 많아야 둘이며 커서가 괄호에 놓인
동안에만 있는 상자에는 그 논리가 이어지지 않는다. 나중에 "왜 여기만 회색을 통과시켰지"
하고 헷갈릴 자리다.

**기본값을 켜자 검사 절반이 무의미해졌다.** "켜면 나온다"를 확인하던 것들인데, 기본값이
이미 그 값이라 노브가 아예 안 도착해도 통과한다. **노브 검사에 보내는 숫자를 전부
기본값과 다르게 바꿨다.** 그리고 `0`으로 끄는 길이 아무 검사에도 안 걸리게 됐는데, 이제는
그게 사람이 실제로 쓰는 설정이라 섹션을 하나 더 붙였다. **기본값을 바꿀 때는 그 값을
전제하던 검사를 같이 볼 것.**

**`tools/smoke.js`에 새 검사는 맨 뒤에 붙일 것.** `run()`이 페이로드가 붙잡고 있는 전역
스텁을 갈아치우므로, 중간에 스텁을 새로 만들면 앞서 만든 것들이 죽는다. 증상이 원인과 두
섹션 떨어진 자리에 나타난다 — save pulse와 toggle이 빨개져서 한참 엉뚱한 데를 봤다.
코드에 주석으로 남겨뒀다.

**두 README를 절반으로 잘랐다.** 591 → 286줄, 516 → 264줄. 지운 것은 "What it costs"
벤치 표 두 개, 토글 브리지 내부 구조, 컨텍스트 키 설계, 기능별 산문이다. **전부
`neon-glow.js` 주석에 이미 같은 내용이 있던 것들이다.** 같은 것을 두 곳에 적으면 한쪽이
반드시 낡는다 — 실제로 두 README 모두 **"마켓플레이스에 없고 앞으로도 올리지 않음"**을
그대로 싣고 있었다. v0.9.11이 올라간 지 한참인데.

**`CLAUDE.md`의 문서 규칙도 같이 고쳤다.** 안 고치면 다음 세션에 도로 불어난다. 기능
하나에 딸린 네 자리 중 "두 README의 산문"이 "`neon-glow.js`의 그 규칙 바로 위 주석"으로
바뀌었다. **README는 쓰는 사람 것, 소스 주석은 고치는 사람 것.**

**커서 아크는 이 페이로드의 성격을 바꾼 첫 기능이다.** 지금까지는 전부 한 번 계산된
정적 CSS였는데, 이건 **작업하는 동안 에디터를 지켜본다** — 화면에 있는 모든 커서 레이어에
MutationObserver를 걸고, 커서의 인라인 `left`/`top`을 문자열로 읽어 비교한다(레이아웃
비용이 없다). 그리는 프레임에서만 사각형을 잰다. 만들면서 걸린 것 셋을 적어둔다.

- **`innerHTML`을 쓸 수 없다.** 워크벤치가 Trusted Types를 강제해서 문자열을 대입하면
  예외가 난다. 전부 `createElementNS`로 만들어야 한다. 지금까지 이걸 안 밟은 건
  `<style>`에 `textContent`만 넣고 있었기 때문이지 알고 피한 게 아니었다.
- **`.focused`는 붙잡을 수 있는 손잡이가 아니다.** 포커스가 패널이든 DevTools든 다른
  데로 가면 Monaco가 즉시 떼어간다. 게다가 `focusin`을 캡처 단계로 걸면 Monaco가 그
  클래스를 붙이기 **전에** 돌아서, 하필 절대 못 찾는 순간에 찾게 된다. 포커스로 대상을
  고르지 말 것.
- **dash가 경로보다 길면 애니메이션이 거꾸로 간다.** 불꽃을 나르는 `stroke-dashoffset`
  이야기다. 화살표 한 번은 경로가 7px인데 머리 길이 하한이 14px이라, 문턱값을 5로 내린
  순간 짧은 아크가 전부 역방향이 됐다. 머리를 경로 길이로 제한해야 한다.

**두 갈래를 구분하게 된 것이 이번 세션의 제일 큰 소득이다.** 효과를 늘리는 것(스캔라인,
부팅 연출)과 빛나는 표면을 늘리는 것(찾기 결과, 선택 영역)은 같은 "기능 추가"가 아니다.
앞쪽은 얹을 때마다 "테마에서 유도한다"는 한 문장에서 멀어지고, 뒤쪽은 그 문장을 그대로
연장한다. **다음에 뭘 넣을지 정할 때 이 질문부터 할 것 — 이 색이 어디서 오는가.**
커서 잔상이 통과한 것도 테마의 커서 색을 그대로 쓰고 움직임만 바꾸기 때문이었다.

**반투명한 테마 색은 스프레드가 필요하다.** 찾기와 선택 색은 글자 뒤에 깔리는 색이라
테마가 반투명으로 잡아둔다. 그 색을 `box-shadow`로 블러만 시키면 거의 보이지 않는다.
`spread`로 원래 폭만큼 먼저 밀어낸 다음 번지게 해야 읽힌다. 커서 색은 불투명이라
스프레드가 없다 — 나중에 왜 여기만 다른지 헷갈릴 자리라 코드에 주석으로 남겼다.

**값은 콘솔에서 골랐다.** DevTools 콘솔에 스타일을 주입하면 재패치도 재시작도 없이
진짜 에디터·진짜 테마 위에서 즉시 보인다. 커서 잔상을 130ms로 잡았다가 답답해서
80 → 60 → 45로 내려간 것이 그 자리에서 끝났다. **페이로드를 고치기 전에 콘솔에서 먼저
볼 것.** 말로 설명하면 감이 안 오고, 재패치를 거치면 한 번 볼 때마다 재시작이다.
찾기 결과를 처음 볼 때 `.cmd`의 `echo`로 시험한 것이 실패 사례다 — 이미 빛나는 키워드
위에 빛을 얹은 것이라 차이가 안 보였다. **안 빛나는 자리에서 시험할 것.**

**흔들림의 비용은 효과가 아니라 구현이 정한다.** 처음에는 "저장할 때마다 87ms 블러를
몇 프레임씩 되풀이한다"며 이 항목을 자르라고 했는데, 그건 `setInterval`로 인라인
transform을 쓰는 데모 기준이었다. `transform`만 건드리는 CSS 키프레임에 `will-change`를
붙이면 워크벤치가 잠깐 컴포지터 레이어로 올라가고, 150ms 동안 이미 가진 텍스처를
옮기기만 한다. 글로우는 한 번도 다시 그려지지 않는다. 눈에 보이는 효과는 같은데 비싼
구현과 공짜 구현이 갈린다.

**렌더러로 이벤트를 즉시 보낼 방법은 상태바 라벨 하나뿐이다.** 페이로드는 워크벤치 안에
살아서 `onDidSaveTextDocument`를 못 듣고, `state.json`은 800~1500ms 폴링이라 Ctrl+S에
대한 반응으로 쓸 수 없다. 그래서 라벨 뒤에 U+200B를 0~3개 붙여 보낸다. 폭이 0이고,
`trim()`에 지워지지 않고, 스크린 리더도 읽지 않는다. **VS Code가 이 문자를 DOM까지
살려 보내는지가 이번 작업의 유일한 미검증 위험이었고, 실기에서 통과했다.**

**스캔라인은 넣지 않았다.** 기술적으로는 제일 싸다 — 정적 오버레이 하나라 스크롤당
비용이 0이다. 넣지 않은 이유는 성능이 아니라 위의 두 갈래 구분이다. 부팅 연출도 같은
이유로 보류했고, 더해서 창을 열 때마다 값을 치른다 — 신기함은 한 번이고 지연은 매번이다.
**터미널 글로우**도 후보였는데, xterm이 캔버스에 그려서 CSS가 안 닿는다.
`terminal.integrated.gpuAcceleration: "off"`를 켜야 DOM 렌더러로 떨어지는데, 남의 설정을
요구하면서 터미널 성능까지 내리는 교환이라 접었다.

**마켓플레이스 게시는 자기 워크플로로 나간다** — `.github/workflows/marketplace.yml`.
릴리스 워크플로에 job을 더하지 않고 파일을 나눈 이유는 실패의 성격이 다르기 때문이다.
릴리스 에셋은 다시 빌드해 올리면 되지만, 마켓에 도달한 버전은 그 번호로 다시 못 올린다.

**그 판단이 첫 게시에서 바로 값을 했다.** 태그를 밀었을 때 아직 `VSCE_PAT` 시크릿이
없어서 마켓 워크플로가 10초 만에 실패했는데, 릴리스 워크플로는 그 옆에서 정상으로 돌아
에셋 셋을 다 붙였다. 시크릿을 넣은 뒤 **태그를 옮기거나 버전을 올리지 않고 같은
`v0.9.8` 태그에 `workflow_dispatch`로 다시 돌려서** 게시했다. 그 기능은 "토큰이
만료됐을 때 태그는 이미 밀린 지 오래"를 위해 넣었는데, 만료가 아니라 첫 설정에서 먼저
쓰였다.

앞으로는 **버전 올려 커밋하고 `node tools/release.js --push` 한 번이면 GitHub 릴리스와
마켓 게시가 같이 나간다.** 퍼블리셔는 `Ruminem`, 확장 ID는 `Ruminem.vscode-neon-glow`.
**PAT은 발급 시점부터 최대 1년이라 2027년 안에 만료된다** — 그때 게시만 빨갛게 실패하면
원인이 그거다. 새 토큰은 반드시 **All accessible organizations**로 발급할 것. 특정 조직으로
잡으면 401이 떨어지는데 메시지가 "토큰이 틀렸다"처럼 나와서 원인을 찾기 어렵다.

**`vsce publish`가 타임아웃 나면 웹으로 올릴 것.** v0.9.11에서 이걸로 하루를 썼다.

`vsce publish`가 `Request timeout: /_apis/gallery`로 죽었다. CI에서 다섯 번, 로컬에서
한 번, 14시간에 걸쳐서. **마켓 관리 화면에서 VSIX를 직접 올리니 한 번에 됐다.**

게시자 페이지(marketplace.visualstudio.com/manage/publishers/Ruminem)에서 확장 **이름
오른쪽의 `⋮`** → Update. 확장 하나를 눌러 들어간 통계 화면의 `...`에는 없다. 올릴
파일은 **릴리스에 붙은 vsix를 받을 것** — 다시 빌드하면 마켓과 릴리스가 다른 바이트가
된다.

**어제 세운 가설 두 개가 둘 다 틀렸다. 다음에 같은 데서 헤매지 말 것.**

- "인증 실패면 401이 즉시 온다" — **아니다.** 인증이 안 되면 갤러리는 302로 로그인
  페이지에 보내고, 클라이언트가 거기서 멎으면 **타임아웃처럼 보인다.** 토큰을 의심할
  거면 추측하지 말고 `vsce verify-pat Ruminem`을 쓸 것. 워크플로에 단계로 넣어뒀고,
  실제로 5초 만에 토큰 가설을 지웠다.
- "마켓 쓰기 경로가 죽었다" — **아니다.** 같은 시각 다른 게시자들은 3분·5분·7분 전에
  계속 올리고 있었다. 확인하는 법은 마켓 조회 API에 `filterType:8`(VS Code 대상),
  `sortBy:1`(최근 갱신순)로 물어보고 맨 위 항목들의 `lastUpdated`를 보는 것이다.
  **"나만 안 되는가"를 부작용 없이 가르는 유일한 방법이다** — 시험 삼아 다른 확장을
  진짜로 게시하는 것은 하지 말 것.

**왜 API만 막혔는지는 끝내 모른다.** 같은 계정, 같은 토큰(검증 통과), 같은 파일인데
경로만 다르면 갈렸다. 더 파려면 시험 게시를 해야 해서 거기서 멈췄다. 다음에 또 나면
**원인을 파지 말고 웹으로 올릴 것.**

**v0.10.0에서 또 났는데, 죽은 자리가 달랐다.** `publish`가 아니라 **`verify-pat` 자체가**
3분 11초 만에 `Request timeout: /_apis/securityroles`로 죽었다. v0.9.11 때는 바로 이
단계가 5초에 통과해서 토큰 가설을 지워줬었다. **위의 "원인 파지 말고 웹으로" 규칙은
토큰이 검증을 통과한다는 전제 위에 세운 것이고, 그 전제가 이제 안 맞는다.**

같은 파일에 적어둔 다른 문장이 더 들어맞는다 — 인증이 안 되면 갤러리는 302로 로그인
페이지에 보내고 클라이언트가 거기서 멎으므로 **타임아웃처럼 보인다.** `verify-pat`이
멎는다는 것은 토큰이 더는 받아들여지지 않는다는 쪽 증거다. 만료는 아니다. 발급한 지
하루였다. 남은 후보는 발급 범위다 — **All accessible organizations**로 안 잡혔을 수 있다.

마켓 조회 API로 최근 갱신순을 확인했고 0분·2분·3분 전에 남들이 계속 올리고 있었다.
**갤러리는 멀쩡했다.** 이 판별은 두 번 다 값을 했다.

**다음 릴리스 전에 PAT을 다시 발급해둘 것.** Marketplace: Manage + All accessible
organizations. 태그를 밀고 나서 알게 되면 또 웹으로 올려야 한다. 지금 v0.10.0은 웹으로
올렸다.

**다음 할 것 — 제일 먼저 재패치해서 v0.10.0을 실기에서 볼 것.** 이번 세션의 셋은
콘솔에 CSS를 주입해 눈으로 봤고 `tools/smoke.js`가 스텁 DOM에서 페이로드를 돌렸지만,
**진짜 워크벤치에 이 페이로드가 실린 적이 없다.** 던지는 문제는 smoke가 잡고 있어서
위험은 낮지만 그걸 알고 낸 릴리스다.

그다음 후보가 둘 있다. **`neonGlow.preset` 퀵픽** — 설정이 열여섯 개가 돼서 새로 깐
사람이 뭘 만져야 할지 모른다. 은은/기본/다 켜기 셋만 두면 되고, 값이 `state.json`을 타고
가므로 **페이로드를 안 건드려 재패치가 없다.** 여기 있는 후보 중 제일 싸다.
**진단 물결선**은 값은 크지만(에러가 어디 있는지 스크롤바 안 뒤지고 보인다) 실패할 수
있다 — 물결이 배경 이미지가 아니라 `border-bottom`이라 `box-shadow`를 걸면 단어 상자
전체가 빨개진다. 아래로 offset 준 그림자로 흉내내야 하고, 그게 물결로 읽힐지 사각형
얼룩으로 읽힐지는 **콘솔에서 봐야 안다.** 요소 수가 뷰포트에도 커밋 diff에도 안 묶이고
진단 개수에 비례하는 것도 이것뿐이다.

**여전히 맥북에서 macOS 경로를 돌려보는 것도 남아 있다.** 네 세션째 바뀐 게 없다.
서명된 `.app` 내부를 고친 뒤 Gatekeeper와 hardened runtime이 어떻게 반응하는지가 유일한
미지수고, "손상되어 열 수 없음"이 뜨면 macOS 지원은 접어야 한다.

**`tools/smoke.js`도 들어왔다** — 네 세션 동안 만들었다 버리기를 반복하던 그것이다.
스텁 DOM에 페이로드를 올려 22가지를 검사하고, 실패하면 종료 코드가 1이다. 넣자마자
값을 했다: 폴링 백오프를 붙이면서 `document.addEventListener`를 페이로드 한가운데
무방비로 놓았는데, 거기서 던지면 **그 뒤 첫 페인트까지 통째로 죽는다**는 것을 커밋 전에
잡아냈다. 그 상황이 검사 항목으로 박혀 있다(`crippled` 스텁).

**통과만 하는 검사는 검사가 아니므로 확인했다.** 가드를 일부러 떼고 돌려서 그 항목이
실제로 빨개지는 것을 보고 되돌렸다. 새 검사를 넣을 때도 같은 절차를 밟을 것.

**워크플로 CI는 태그 없이도 시험할 수 있다.** 마켓 워크플로를 `main`에 대고
`workflow_dispatch`로 돌리면 체크아웃과 setup-node를 실제로 태운 뒤 태그 검사에서
멈춘다 — 게시까지 가지 않으므로 되돌릴 수 없는 일이 일어나지 않는다. 액션 버전을
v4에서 v7로 올릴 때 이 방법으로 확인했다. 일부러 빨간 실행이 하나 남지만, 릴리스를
내보내고 나서야 CI가 깨진 걸 아는 것보다 낫다.

**글로우에서 눈에 보이는 것은 총량이 아니라 비율이다.** 네 겹이 전부 글자 바로 옆에도
칠하는데(블러는 원본 자리에서 가장 진하다), 옛 알파는 거기서 2.75로 더해졌다. 1.0을
넘으면 다 같은 불투명이라 **가장 넓은 겹이 가장 좁은 겹만큼 진했고**, 그래서 눈이 경계를
찾을 기울기가 없었다. 밝기를 도로 올려도 선명함이 유지되는 것을 보고 확인했다 — 어두워져서
좋아진 게 아니었다. 다음에 글로우 모양을 손볼 일이 있으면 **총량 말고 안팎 비율을 볼 것.**

**`tools/bench.js`를 만들었다.** CDP로 스크롤을 구동하면서 두 공식을 번갈아 적용하고
이웃한 쌍끼리 비교한다. 의존성 없이 노드 내장 `WebSocket`과 `fetch`만 쓴다. 이번 측정은
네 쌍 전부 −56~−57%로 흔들림이 없었다.

두 함정이 시간을 먹었으니 적어둔다. 하나, **VS Code 통합 터미널에는
`ELECTRON_RUN_AS_NODE=1`이 걸려 있다.** 그 상태로 `Code.exe`를 띄우면 에디터가 아니라
Node로 뜨고, 모든 VS Code 플래그에 "bad option"을 뱉으면서 즉시 죽는다 — 플래그가 틀린
것처럼 읽혀서 한참 헤맸다. 둘, **`/json/list`는 `id`, CDP 명령은 `targetId`다.** 같은
대상을 다르게 부르고, 틀리면 필드 이름이 없는 역직렬화 오류만 나온다.

**막힌 것** — macOS/Linux 실기가 없다. **Windows 11에서만** 검증됐고, 나머지 두
플랫폼은 여전히 "동작하도록 작성했을 뿐"이다. 그리고 v0.10.0의 페이로드가 아직 실기에서
한 번도 안 돌았다 — 위의 "다음 할 것" 첫 줄이다.

**보류** — 로더 방식. 번들에는 로더만 심고 실제 페이로드는 확장 폴더에서 `vscode-file`로
불러오면 재패치가 영구히 사라진다. 대신 시작할 때 페이로드를 가져와야 해서 글로우가 늦게
뜨는 구간이 생긴다. 자주 겪는 불편을 줄이려다 항상 겪는 불편을 만드는 교환이라 그대로
보류다.
