# NEXT

**여기까지 됨 (2026-09-14, v0.17.1 뒤 미릴리스)** — 토큰 색 개수가 같은 테마로 바꾸면 글로우가 옛 테마 색에 남던 버그를 고쳤고(smoke 검사 추가), `chromeStyles()`를 표면별 함수로 나눴고(설정 조합 6개에서 출력 바이트 동일), 안 쓰던 이미지 둘을 지웠고, 이 파일에 쌓인 세션 일지 중 오래갈 것은 CLAUDE.md `## 알아둘 것`으로 옮겼다(전문은 `git log -p -- NEXT.md`).
**다음 할 것** — 테마 수정을 0.17.2로 낼지 정할 것(페이로드만 바뀌어 재패치 없음). `brightness()` 숨쉬기 네 자리는 아직 안 쟀고, 이름 바꾸기 상자 숨쉬기를 컴포지터로 넘길 수 있는지도 미확인. 후보: 테마 색이 검정일 때 커서 색으로 대체, 인라인 제안(고스트 텍스트) 글로우.
**막힌 것** — macOS/Linux 실기 없음(macOS는 서명된 `.app`을 고친 뒤 Gatekeeper 반응이 미지수). 디버그 멈춘 줄·중단점 글로우는 화면으로 본 적 없음. 마켓 재시도 경로가 실제로 도는지 아직 모름.
