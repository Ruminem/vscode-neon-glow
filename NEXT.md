# NEXT

**여기까지 됨** — 릴리스는 `v0.6.0`까지 나갔고 로컬은 **0.9.0**이다. 이번 세션은
**실제 대형 C++ 파일에서 성능을 실측**하고 **멀티플랫폼 지원을 정리**한 두 덩어리다.

**성능 — 산수가 틀렸다는 걸 실측이 뒤집었다.** VS Code를 `--remote-debugging-port`로
띄우고 Chrome DevTools Protocol로 붙어, `nlohmann/json`의 27,000줄 단일 헤더를 열고
스크롤을 구동하면서 컴포지터의 `RasterTask` 시간을 합산했다. 화면에 토큰 span 390개,
그중 220개가 빛나는 상태에서 **글로우 없음 12ms / 2겹 16ms / 3겹 87ms**. 면적 산수는
가장 넓은 겹이 전체의 ⅔라고 예측했지만 실제로는 **사실상 전부**였다. 블러 시간이
반경보다 훨씬 빠르게 오르고, 36px에서는 이웃 토큰끼리 블러가 대량으로 겹친다.

측정 방법론 자체가 함정이었다. 캐시가 데워지면서 래스터 시간이 계속 아래로 흘러서,
단일 측정으로는 −33%와 −10%를 구분하지 못한다. **같은 변형을 바로 옆에 붙여 쌍으로
재야** 드리프트가 상쇄된다. 처음에 "괄호 규칙이 ⅓을 먹는다"고 결론냈다가 쌍대 재측정에서
−10%로 정정한 것이 그 사례다.

레버 두 개를 설정으로 뺐다. `glowLayers`(겹 수, 3→2가 −74%)와 `maxBlur`(반경 상한,
28px에서 −34%). **기본값은 둘 다 지금과 동일하다.** 켜기 전까지 아무것도 바뀌지 않는다.
공짜 개선은 없었다 — `backface-visibility` 제거는 노이즈 안이었고(Chromium 148에서는
죽은 코드다), 비용은 블러 면적 그 자체다.

**멀티플랫폼** — 확장이 경로를 추측하지 않고 `vscode.env.appRoot`를 쓴다. 포터블 빌드나
옮겨진 접두사도 구조적으로 맞는다. snap/flatpak은 **찾아서 거부한다** — 읽기 전용
마운트라 어떤 권한으로도 패치가 불가능한데, 그동안 "설치를 못 찾음"으로 나왔다.
`install.sh`를 추가하고 릴리스에 `tar.gz`로 붙였다. 줄바꿈은 `.gitattributes`로
양쪽 고정(`.cmd`=CRLF, `.sh`=LF)했고 워크플로가 어긋나면 빌드를 실패시킨다.

**잡은 버그들** — 괄호 색이 한 색으로 뭉치던 것은 우리가 `color`에 `!important`를 붙여
VS Code의 `bracket-highlighting`(클래스 2개, 더 높은 특이도)을 힘으로 덮어써서였다.
리로드 직후 팔레트가 이미 빛나는 에디터 위에 `Enable`을 내밀던 것은 `when` 절이 unset과
false를 구분하지 못해서였고, 긍정형 키 둘(`neonGlow.on`/`off`)로 바꿨다. `Show status`에
"Restore the original bundle"이 **유일한 버튼**으로 붙어 있어 눌러보면 설치가 통째로
되돌아갔다 — 읽는 화면에 파괴적 동작을 놓은 내 설계 실수였다.

**다음 할 것 — 맥북에서 macOS 경로를 실제로 돌려보는 것.** 코드로만 작성했고 아무도
실행하지 않았다. 확인할 것 둘: (1) `install.sh`가 실제로 VS Code와 `code` CLI를 찾아
패치까지 가는지, (2) **서명된 `.app` 내부를 고친 뒤 VS Code가 정상 실행되는지.**
Gatekeeper와 Apple Silicon의 hardened runtime이 어떻게 반응하는지가 유일한 미지수다.
"손상되어 열 수 없음"이 뜨면 macOS 지원은 접어야 한다.

그리고 **`v0.9.0` 태그**. 0.7.0~0.9.0이 릴리스되지 않았다. 태그를 밀면 CI가 VSIX와
zip/tar.gz를 만들어 붙인다. 페이로드 스탬프가 `13dca68b687e`로 바뀌었으니 기존
사용자는 재패치가 필요하다.

**보류** — 로더 방식. 번들에는 로더만 심고 실제 페이로드는 확장 폴더에서 `vscode-file`로
불러오면 **재패치가 영구히 사라진다**(`state.json`을 같은 방식으로 읽고 있으니 메커니즘은
증명됨). 대신 시작할 때 페이로드를 가져와야 해서 글로우가 늦게 뜨는 구간이 생긴다.
자주 겪는 불편을 줄이려다 항상 겪는 불편을 만드는 교환이라 보류했다.

검증 스크립트(패치 왕복, 렌더 스모크, 문서 일관성 감사, CDP 벤치마크)는 스크래치
디렉터리에만 있었고 세션과 함께 사라진다. 원격에서 이어서 작업할 거면 `tools/`에
넣어두는 편이 낫다.

**막힌 것** — macOS/Linux 실기가 없다. **Windows 11 / VS Code 1.136.1에서만** 검증됐고,
나머지 두 플랫폼은 "동작하도록 작성했을 뿐"이다.
